TESTS=@TESTS@

.PHONY:$(TESTS)

all: $(TESTS)
$(TESTS):
	@echo "-- Building test for $@"; $(MAKE) -s -C $@; if test $$? != 0; then exit $$?; fi 

clean:
	@for i in $(TESTS); do $(MAKE) -s -C $$i clean; done;

