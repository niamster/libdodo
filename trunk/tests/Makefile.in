all:
	@for i in *; do if [ -d $$i ]; then echo "-- Building test for $${i%_test}"; $(MAKE) -s -C $$i; if [[ $$? != 0 ]]; then exit $$?; fi; fi; done;

clean:
	@for i in *; do if [ -d $$i ]; then $(MAKE) -s -C $$i clean; fi; done;

